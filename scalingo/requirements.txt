Flask==3.0.3
gunicorn==22.0.0
requests==2.32.3
Flask-Limiter==3.8.0
Pillow==10.4.0
rembg[cpu]==2.0.59

# --- Greffe du visage sur le corps (pipeline texture_swap) ---
# mediapipe : détection des landmarks faciaux du corps (rendu logiciel).
#   Wheels cp312 dispo à partir de 0.10.14 ; pin numpy<2 (imposé par mediapipe).
#   Tire opencv-contrib-python -> nécessite libGL au runtime (voir Aptfile).
numpy<2
scipy>=1.11,<2
trimesh>=4.4,<5
mediapipe==0.10.14
